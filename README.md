DevJourney Log 6/11/26

Hello world!

This is a firt-time project of mine that I created for my organization, so I wouldn't recommend running it on a machine.

It's just a simple script that runs through /C and clears out old Users. Note that it does not have a 'dry-run' mode or conformation, it just does it. So be careful.
(Hopefully no one will ever see this anyway)

I'll probably make a Python version instead that is a bit more configurable. Maybe. Who knows?

TLDR; this is just a temp project. I really wouldn't use this on your machine.
